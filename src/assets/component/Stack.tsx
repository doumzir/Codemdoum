import TextType from "./TextType";


const Stack = () => {
    return (
        <div>
            <TextType Title="What’s stack ? 🧐">Curious Front end developer, i want to explore every JS stack </TextType>
        </div>
    );
};

export default Stack;